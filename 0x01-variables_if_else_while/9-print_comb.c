#include <stdio.h>
/**
 * main - Entry point
 * Description: 'Print numbers using putchar'
 * Return: Always 0 (Success)
 */

inf main(void)
{
	int a = 0;

	while (a < 10)
	{
		putchar(a + '0');
		putchar(', ');
		a++;
	return (0);
}
