#include <stdio.h>
/**
 * main - Entry point
 * Description: 'Print Base 10 Numbers'
 * Return: Always 0 (Success)
 */

int main(void)
{
	int a = 0;

	while (a < 10)
	{
		printf("%d\n", a);
		a++;
	}
	return (0);
}
