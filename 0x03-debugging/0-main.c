#include "main.h"
/**
 * main - Entry point
 * Description: 'Positive_Negative()'
 * Return: Always 0 (Success)
 */

int main(void)
{
	int i;

	if (i == 0)
		printf("0");
	else
		positive_or_negative(i);

	return (0);
}
